namespace ImpactRush.Core
{
    /// <summary>
    /// Build-order scene identifiers. Keep values aligned with Editor Build Settings.
    /// </summary>
    public enum GameScene
    {
        Bootstrap = 0,
        MainMenu = 1,
        Gameplay = 2
    }
}
